# Propyte CRM — MCP Admin (F0 + F1) Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Entregar la fundación del MCP admin del CRM (auth Bearer + route `/api/mcp` + usuario-sistema + wrapper stdio con introspección) y el caso estrella: crear/editar flujos (automation rules, action plans, routing, SLA) desde lenguaje natural.

**Architecture:** Dos piezas espejo de `propyte-hub-mcp`. (A) En el CRM, un route dinámico `src/app/api/mcp/[...path]/route.ts` autentica una sola vez contra `CRM_MCP_API_TOKEN`, resuelve un usuario-sistema SUPER_ADMIN y despacha a handlers que llaman a Prisma/capa server directamente, registrando todo en `AuditLog`. Sin DELETE. (B) Un repo nuevo `propyte-crm-mcp` (stdio) expone tools que pegan al REST del CRM con `Authorization: Bearer`.

**Tech Stack:** CRM: Next.js 14, Prisma, zod, vitest. Wrapper: TypeScript ES2022, `@modelcontextprotocol/sdk@^1.29`, zod, tsx, node `--test`.

**Spec:** `docs/superpowers/specs/2026-06-19-propyte-crm-mcp-admin-design.md`

**Convención de respuesta del CRM:** todos los handlers MCP responden `{ data: ... }` en éxito y `{ error: string, details?: unknown }` con status ≥400 en error. El wrapper (`client.ts`) ya lee `error`/`message`.

**Paths de REST que expone el route** (segmentos de `[...path]`):
- `GET  /api/mcp/health`
- `GET  /api/mcp/schema`
- `GET  /api/mcp/users`
- `GET  /api/mcp/teams`
- `GET  /api/mcp/automation/rules` · `POST /api/mcp/automation/rules` · `GET /api/mcp/automation/rules/:id` · `PATCH /api/mcp/automation/rules/:id`
- `GET  /api/mcp/automation/plans` · `POST /api/mcp/automation/plans` · `GET /api/mcp/automation/plans/:id` · `PATCH /api/mcp/automation/plans/:id`
- `GET  /api/mcp/automation/routing` · `POST /api/mcp/automation/routing` · `PATCH /api/mcp/automation/routing/:id`
- `GET  /api/mcp/automation/sla` · `POST /api/mcp/automation/sla` · `PATCH /api/mcp/automation/sla/:id`
- `GET  /api/mcp/automation/queue` · `POST /api/mcp/automation/queue/:id/retry`

---

## File Structure

**CRM (`propyte-crm/`):**
- Create `src/lib/mcp/auth.ts` — valida Bearer contra `CRM_MCP_API_TOKEN` (timing-safe) + resuelve usuario-sistema.
- Create `src/lib/mcp/respond.ts` — helpers `ok()`/`fail()` + `writeAudit()`.
- Create `src/lib/mcp/schema-introspection.ts` — payload de `describe_schema` (enums vivos + forma del DSL).
- Create `src/lib/mcp/handlers/introspection.ts` — health, schema, users, teams.
- Create `src/lib/mcp/handlers/automation.ts` — rules, plans, routing, sla, queue.
- Create `src/lib/mcp/dispatch.ts` — tabla `${METHOD} /seg/seg` → handler.
- Create `src/app/api/mcp/[...path]/route.ts` — GET/POST/PATCH → auth → dispatch.
- Create `scripts/seed-mcp-user.ts` — upsert de `mcp@propyte.local` (ADMIN).
- Test `src/lib/mcp/auth.test.ts`, `src/lib/mcp/dispatch.test.ts`.

**Wrapper (`../propyte-crm-mcp/`, repo nuevo hermano):**
- Create `package.json`, `tsconfig.json`, `.gitignore`, `README.md`.
- Create `src/client.ts` — `CrmClient` (idéntico al `HubClient`).
- Create `src/index.ts` — server stdio + ListTools/CallTool.
- Create `src/schemas.ts` — zod de inputs.
- Create `src/tools/*.ts` + `src/tools/index.ts` — registro `ALL_TOOLS`.
- Test `tests/tools.test.ts`.

---

# FASE 0 — Fundación

## Task 0.1: Usuario-sistema MCP (CRM)

**Files:**
- Create: `scripts/seed-mcp-user.ts`

- [ ] **Step 1: Escribir el seed** modelado en `scripts/seed-admin-users.ts` (copia su forma de `prisma.user.upsert` y los campos requeridos del modelo `User`: name, email, role, passwordHash, etc.). Valores fijos:

```ts
// scripts/seed-mcp-user.ts
import prisma from "@/lib/db";
import bcrypt from "bcryptjs";

async function main() {
  const email = "mcp@propyte.local";
  // Sin login interactivo: hash de un secreto aleatorio inutilizable.
  const passwordHash = await bcrypt.hash(crypto.randomUUID(), 10);
  const user = await prisma.user.upsert({
    where: { email },
    update: { role: "ADMIN", isActive: true },
    create: { email, name: "MCP Admin", role: "ADMIN", passwordHash, isActive: true },
  });
  console.log("MCP system user:", user.id);
}
main().then(() => process.exit(0)).catch((e) => { console.error(e); process.exit(1); });
```

> Si el modelo `User` exige campos adicionales (revisar `scripts/seed-admin-users.ts` y `prisma/schema.prisma`), agrégalos al `create` con los mismos valores que usa ese seed para un ADMIN.

- [ ] **Step 2: Ejecutar**

Run: `npx tsx scripts/seed-mcp-user.ts`
Expected: imprime `MCP system user: <uuid>`.

- [ ] **Step 3: Commit**

```bash
git add scripts/seed-mcp-user.ts
git commit -m "feat(mcp): seed usuario-sistema mcp@propyte.local"
```

## Task 0.2: Auth Bearer del MCP (CRM)

**Files:**
- Create: `src/lib/mcp/auth.ts`
- Test: `src/lib/mcp/auth.test.ts`

- [ ] **Step 1: Test que falla**

```ts
// src/lib/mcp/auth.test.ts
import { describe, it, expect } from "vitest";
import { checkBearer } from "./auth";

describe("checkBearer", () => {
  const token = "tok_abc123";
  it("acepta el token correcto", () => {
    expect(checkBearer(`Bearer ${token}`, token)).toBe(true);
  });
  it("rechaza token incorrecto", () => {
    expect(checkBearer("Bearer wrong", token)).toBe(false);
  });
  it("rechaza header faltante o mal formado", () => {
    expect(checkBearer(null, token)).toBe(false);
    expect(checkBearer("Token x", token)).toBe(false);
  });
  it("tolera espacios/newline trailing en el token recibido", () => {
    expect(checkBearer(`Bearer ${token}\n`, token)).toBe(true);
  });
});
```

- [ ] **Step 2: Correr y ver fallar**

Run: `npm test -- src/lib/mcp/auth.test.ts`
Expected: FAIL — `checkBearer is not a function`.

- [ ] **Step 3: Implementar**

```ts
// src/lib/mcp/auth.ts
import { timingSafeEqual } from "crypto";
import prisma from "@/lib/db";

export function checkBearer(header: string | null, expected: string): boolean {
  if (!expected) return false;
  if (!header?.startsWith("Bearer ")) return false;
  const got = header.slice(7).trim(); // gotcha conocido: \n trailing
  const a = Buffer.from(got);
  const b = Buffer.from(expected);
  if (a.length !== b.length) return false;
  return timingSafeEqual(a, b);
}

let cachedUserId: string | null = null;
const SYSTEM_EMAIL = "mcp@propyte.local";

/** id del usuario-sistema MCP para AuditLog. Lanza si no existe (correr seed). */
export async function getMcpUserId(): Promise<string> {
  if (cachedUserId) return cachedUserId;
  const u = await prisma.user.findUnique({ where: { email: SYSTEM_EMAIL }, select: { id: true } });
  if (!u) throw new Error("MCP system user missing: run scripts/seed-mcp-user.ts");
  cachedUserId = u.id;
  return u.id;
}
```

- [ ] **Step 4: Correr y ver pasar**

Run: `npm test -- src/lib/mcp/auth.test.ts`
Expected: PASS (4 tests).

- [ ] **Step 5: Commit**

```bash
git add src/lib/mcp/auth.ts src/lib/mcp/auth.test.ts
git commit -m "feat(mcp): auth Bearer timing-safe + usuario-sistema"
```

## Task 0.3: Helpers de respuesta y audit (CRM)

**Files:**
- Create: `src/lib/mcp/respond.ts`

- [ ] **Step 1: Implementar** (sin test propio; se ejercita vía dispatch)

```ts
// src/lib/mcp/respond.ts
import { NextResponse } from "next/server";
import prisma from "@/lib/db";

export function ok(data: unknown, status = 200) {
  return NextResponse.json({ data }, { status });
}
export function fail(error: string, status = 400, details?: unknown) {
  return NextResponse.json({ error, ...(details ? { details } : {}) }, { status });
}

/** Registra una mutación MCP en audit_logs. No lanza. */
export async function writeAudit(
  userId: string,
  action: "CREATE" | "UPDATE",
  entity: string,
  entityId: string,
  changes: Record<string, unknown>
) {
  await prisma.auditLog
    .create({ data: { userId, action, entity, entityId, changes: { source: "mcp", ...changes } } })
    .catch(() => null);
}
```

- [ ] **Step 2: Commit**

```bash
git add src/lib/mcp/respond.ts
git commit -m "feat(mcp): helpers ok/fail/writeAudit"
```

## Task 0.4: describe_schema (CRM)

**Files:**
- Create: `src/lib/mcp/schema-introspection.ts`

- [ ] **Step 1: Implementar** (payload estático tomado de enums vivos del CRM)

```ts
// src/lib/mcp/schema-introspection.ts
import { workflowActionTypes } from "@/lib/validations/rebuild-f1";

export function describeSchema() {
  return {
    triggerTypes: ["EVENT", "TIME", "BEHAVIORAL", "INACTIVITY", "STAGE_CHANGE", "SLA_BREACH", "SCORE_THRESHOLD"],
    eventTypesHint: ["lead.captured", "lead.assigned", "deal.stage_changed", "whatsapp.replied"],
    workflowActionTypes,
    leadAssignmentModes: ["ROUND_ROBIN", "PERFORMANCE", "MANUAL", "GUARDIA"],
    connectorProviders: [
      "META", "INSTAGRAM", "MESSENGER", "TIKTOK", "WEBSITE", "ZAPIER", "MANUAL",
      "GOOGLE", "LINKEDIN", "INMUEBLES24", "LAMUDI_PROPPIT", "PROPIEDADES",
      "VIVANUNCIOS", "EASYBROKER", "CUSTOM",
    ],
    roles: ["ADMIN", "DIRECTOR", "GERENTE", "TEAM_LEADER", "MARKETING", "ASESOR", "BROKER"],
    plazas: ["PDC", "TULUM", "MERIDA"],
    conditionsDsl: {
      shape: "ConditionGroup recursivo",
      examples: [
        { all: [{ field: "source", op: "eq", value: "META" }, { field: "plaza", op: "eq", value: "TULUM" }] },
        { any: [{ field: "score", op: "gte", value: 70 }] },
      ],
      note: "Validado server-side por conditionsDslSchema (@/lib/validations/rebuild-f1).",
    },
    actionSpec: { shape: "{ type: WorkflowActionType, config: object }[]", min: 1 },
  };
}
```

- [ ] **Step 2: Commit**

```bash
git add src/lib/mcp/schema-introspection.ts
git commit -m "feat(mcp): describe_schema con enums vivos"
```

## Task 0.5: Handlers de introspección (CRM)

**Files:**
- Create: `src/lib/mcp/handlers/introspection.ts`

- [ ] **Step 1: Implementar**

```ts
// src/lib/mcp/handlers/introspection.ts
import prisma from "@/lib/db";
import { describeSchema } from "../schema-introspection";

export async function health() {
  const dbOk = await prisma.$queryRaw`SELECT 1`.then(() => true).catch(() => false);
  return { ok: dbOk, env: process.env.NODE_ENV ?? "unknown", service: "propyte-crm-mcp" };
}
export async function schema() {
  return describeSchema();
}
export async function listUsers() {
  return prisma.user.findMany({
    where: { isActive: true },
    select: { id: true, name: true, email: true, role: true },
    orderBy: { name: "asc" },
  });
}
export async function listTeams() {
  return prisma.team.findMany({
    select: { id: true, name: true, plaza: true, leaderId: true },
    orderBy: { name: "asc" },
  });
}
```

> Si el modelo `Team` usa otro nombre de campo para plaza/líder, ajusta el `select` a los campos reales del `prisma/schema.prisma`.

- [ ] **Step 2: Commit**

```bash
git add src/lib/mcp/handlers/introspection.ts
git commit -m "feat(mcp): handlers health/schema/users/teams"
```

## Task 0.6: Dispatcher + route (CRM)

**Files:**
- Create: `src/lib/mcp/dispatch.ts`
- Create: `src/app/api/mcp/[...path]/route.ts`
- Test: `src/lib/mcp/dispatch.test.ts`

- [ ] **Step 1: Test que falla** (resolución de rutas, sin tocar DB)

```ts
// src/lib/mcp/dispatch.test.ts
import { describe, it, expect } from "vitest";
import { resolveRoute } from "./dispatch";

describe("resolveRoute", () => {
  it("resuelve introspección", () => {
    expect(resolveRoute("GET", ["health"])?.key).toBe("GET /health");
    expect(resolveRoute("GET", ["schema"])?.key).toBe("GET /schema");
  });
  it("resuelve colecciones y :id", () => {
    expect(resolveRoute("POST", ["automation", "rules"])?.key).toBe("POST /automation/rules");
    const r = resolveRoute("PATCH", ["automation", "rules", "abc"]);
    expect(r?.key).toBe("PATCH /automation/rules/:id");
    expect(r?.params.id).toBe("abc");
  });
  it("retorna null si no existe la ruta", () => {
    expect(resolveRoute("DELETE", ["automation", "rules", "abc"])).toBeNull();
    expect(resolveRoute("GET", ["nope"])).toBeNull();
  });
});
```

- [ ] **Step 2: Correr y ver fallar**

Run: `npm test -- src/lib/mcp/dispatch.test.ts`
Expected: FAIL — `resolveRoute is not a function`.

- [ ] **Step 3: Implementar dispatcher**

```ts
// src/lib/mcp/dispatch.ts
import * as intro from "./handlers/introspection";
import * as wf from "./handlers/automation";

type Ctx = { userId: string };
export type Handler = (args: { body: unknown; params: Record<string, string>; ctx: Ctx }) => Promise<unknown>;

// Patrones de ruta: ":id" matchea cualquier segmento.
const ROUTES: Record<string, Handler> = {
  "GET /health": async () => intro.health(),
  "GET /schema": async () => intro.schema(),
  "GET /users": async () => intro.listUsers(),
  "GET /teams": async () => intro.listTeams(),

  "GET /automation/rules": async () => wf.listRules(),
  "POST /automation/rules": async ({ body, ctx }) => wf.createRule(body, ctx.userId),
  "GET /automation/rules/:id": async ({ params }) => wf.getRule(params.id),
  "PATCH /automation/rules/:id": async ({ body, params, ctx }) => wf.updateRule(params.id, body, ctx.userId),

  "GET /automation/plans": async () => wf.listPlans(),
  "POST /automation/plans": async ({ body, ctx }) => wf.createPlan(body, ctx.userId),
  "GET /automation/plans/:id": async ({ params }) => wf.getPlan(params.id),
  "PATCH /automation/plans/:id": async ({ body, params, ctx }) => wf.updatePlan(params.id, body, ctx.userId),

  "GET /automation/routing": async () => wf.listRouting(),
  "POST /automation/routing": async ({ body, ctx }) => wf.createRouting(body, ctx.userId),
  "PATCH /automation/routing/:id": async ({ body, params, ctx }) => wf.updateRouting(params.id, body, ctx.userId),

  "GET /automation/sla": async () => wf.listSla(),
  "POST /automation/sla": async ({ body, ctx }) => wf.createSla(body, ctx.userId),
  "PATCH /automation/sla/:id": async ({ body, params, ctx }) => wf.updateSla(params.id, body, ctx.userId),

  "GET /automation/queue": async () => wf.listQueue(),
  "POST /automation/queue/:id/retry": async ({ params, ctx }) => wf.retryQueue(params.id, ctx.userId),
};

export function resolveRoute(method: string, segments: string[]) {
  for (const key of Object.keys(ROUTES)) {
    const [m, pattern] = key.split(" ");
    if (m !== method) continue;
    const pSegs = pattern.slice(1).split("/");
    if (pSegs.length !== segments.length) continue;
    const params: Record<string, string> = {};
    let match = true;
    for (let i = 0; i < pSegs.length; i++) {
      if (pSegs[i].startsWith(":")) params[pSegs[i].slice(1)] = segments[i];
      else if (pSegs[i] !== segments[i]) { match = false; break; }
    }
    if (match) return { key, handler: ROUTES[key], params };
  }
  return null;
}
```

- [ ] **Step 4: Correr y ver pasar**

Run: `npm test -- src/lib/mcp/dispatch.test.ts`
Expected: PASS (3 tests). (`wf.*` aún no existe → si el import rompe, agrega un stub temporal en automation.ts; se completa en F1. Para mantener verde, crea `src/lib/mcp/handlers/automation.ts` exportando funciones que lanzan `throw new Error("F1")`.)

- [ ] **Step 5: Implementar el route**

```ts
// src/app/api/mcp/[...path]/route.ts
import { NextRequest } from "next/server";
import { checkBearer, getMcpUserId } from "@/lib/mcp/auth";
import { resolveRoute } from "@/lib/mcp/dispatch";
import { ok, fail } from "@/lib/mcp/respond";

export const dynamic = "force-dynamic";

async function handle(req: NextRequest, segments: string[]) {
  const token = process.env.CRM_MCP_API_TOKEN ?? "";
  if (!checkBearer(req.headers.get("authorization"), token)) return fail("unauthorized", 401);

  const route = resolveRoute(req.method, segments);
  if (!route) return fail(`no_route: ${req.method} /${segments.join("/")}`, 404);

  let body: unknown = undefined;
  if (req.method !== "GET") body = await req.json().catch(() => ({}));

  try {
    const userId = await getMcpUserId();
    const data = await route.handler({ body, params: route.params, ctx: { userId } });
    return ok(data, req.method === "POST" ? 201 : 200);
  } catch (e) {
    const msg = (e as Error).message ?? "error";
    const status = msg.includes("not found") || msg.includes("no encontrad") ? 404 : 400;
    return fail(msg, status);
  }
}

type P = { params: Promise<{ path: string[] }> };
export async function GET(req: NextRequest, { params }: P) { return handle(req, (await params).path); }
export async function POST(req: NextRequest, { params }: P) { return handle(req, (await params).path); }
export async function PATCH(req: NextRequest, { params }: P) { return handle(req, (await params).path); }
// Sin DELETE: el route no lo exporta → 405 automático.
```

> Nota Next 14/15: si la versión trata `params` como objeto plano (no Promise), quita `await` y `Promise<>`. Verifica con otra dynamic route del repo (`api/deals/[id]/route.ts`).

- [ ] **Step 6: Verificar build de tipos**

Run: `npm test` (corre toda la suite, debe seguir verde)
Expected: PASS.

- [ ] **Step 7: Commit**

```bash
git add src/lib/mcp/dispatch.ts src/lib/mcp/dispatch.test.ts "src/app/api/mcp/[...path]/route.ts"
git commit -m "feat(mcp): route dinámico /api/mcp + dispatcher sin DELETE"
```

## Task 0.7: Variables de entorno (CRM)

**Files:**
- Modify: `.env.example` (agregar la var, sin valor real)

- [ ] **Step 1: Documentar la var** en `.env.example`:

```
# MCP admin server (stdio wrapper propyte-crm-mcp). Generar: openssl rand -hex 32
CRM_MCP_API_TOKEN=
```

- [ ] **Step 2:** Pedir a Luis poner el valor en `.env` local y en Hostinger (NO commitear el valor). Generar con `openssl rand -hex 32`.

- [ ] **Step 3: Commit**

```bash
git add .env.example
git commit -m "docs(mcp): CRM_MCP_API_TOKEN en .env.example"
```

## Task 0.8: Scaffold del wrapper `propyte-crm-mcp`

**Files (en `../propyte-crm-mcp/`):**
- Create: `package.json`, `tsconfig.json`, `.gitignore`

- [ ] **Step 1: Crear repo y archivos base**

```bash
mkdir -p ../propyte-crm-mcp/src/tools ../propyte-crm-mcp/tests
cd ../propyte-crm-mcp && git init
```

`package.json`:
```json
{
  "name": "propyte-crm-mcp",
  "version": "0.1.0",
  "type": "module",
  "bin": { "propyte-crm-mcp": "dist/index.js" },
  "scripts": {
    "build": "tsc",
    "dev": "tsx src/index.ts",
    "test": "tsx --test tests/*.test.ts",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": { "@modelcontextprotocol/sdk": "^1.29.0", "zod": "^3.23.0" },
  "devDependencies": { "@types/node": "^22.0.0", "tsx": "^4.19.0", "typescript": "^5.6.0" }
}
```

`tsconfig.json` (idéntico al de `propyte-hub-mcp`):
```json
{
  "compilerOptions": {
    "target": "ES2022", "module": "ES2022", "moduleResolution": "Bundler",
    "outDir": "dist", "rootDir": "src", "strict": true, "esModuleInterop": true,
    "skipLibCheck": true, "resolveJsonModule": true, "declaration": false, "sourceMap": true
  },
  "include": ["src/**/*"],
  "exclude": ["node_modules", "dist", "tests"]
}
```

`.gitignore`:
```
node_modules
dist
*.log
```

- [ ] **Step 2: Instalar**

Run: `cd ../propyte-crm-mcp && npm install`
Expected: instala sin errores.

- [ ] **Step 3: Commit**

```bash
git add . && git commit -m "chore: scaffold propyte-crm-mcp"
```

## Task 0.9: Client del wrapper

**Files:**
- Create: `../propyte-crm-mcp/src/client.ts`

- [ ] **Step 1: Implementar** (copia exacta de `HubClient` con nombres CRM; soporta DELETE en el tipo NO — solo GET/POST/PATCH)

```ts
// src/client.ts
export type FetchImpl = typeof fetch;
export interface CrmClientOptions { url: string; token: string; fetchImpl?: FetchImpl; }

export class CrmClient {
  private url: string; private token: string; private fetchImpl: FetchImpl;
  constructor(opts: CrmClientOptions) {
    if (!opts.url) throw new Error("PROPYTE_CRM_URL is required");
    if (!opts.token) throw new Error("CRM_MCP_API_TOKEN is required");
    this.url = opts.url.replace(/\/$/, "");
    this.token = opts.token.trim(); // gotcha \n trailing
    this.fetchImpl = opts.fetchImpl ?? fetch;
  }
  async request<T = unknown>(method: "GET" | "POST" | "PATCH", path: string, body?: unknown): Promise<T> {
    const res = await this.fetchImpl(`${this.url}${path}`, {
      method,
      headers: { Authorization: `Bearer ${this.token}`, "Content-Type": "application/json" },
      body: body === undefined ? undefined : JSON.stringify(body),
    });
    const text = await res.text();
    const data = text ? safeJson(text) : null;
    if (!res.ok) {
      const m = (data as { error?: string; message?: string })?.error
        ?? (data as { message?: string })?.message ?? `HTTP ${res.status}`;
      throw new Error(`${m} (status=${res.status})`);
    }
    return (data as { data?: T })?.data ?? (data as T);
  }
}
function safeJson(t: string): unknown { try { return JSON.parse(t); } catch { return t; } }
```

- [ ] **Step 2: Commit**

```bash
git add src/client.ts && git commit -m "feat: CrmClient (Bearer fetch)"
```

## Task 0.10: Server stdio del wrapper + tools de introspección

**Files:**
- Create: `../propyte-crm-mcp/src/index.ts`
- Create: `../propyte-crm-mcp/src/schemas.ts`
- Create: `../propyte-crm-mcp/src/tools/introspection.ts`
- Create: `../propyte-crm-mcp/src/tools/index.ts`

- [ ] **Step 1: `src/index.ts`** (copia del hub con nombres CRM)

```ts
#!/usr/bin/env node
import { Server } from "@modelcontextprotocol/sdk/server/index.js";
import { StdioServerTransport } from "@modelcontextprotocol/sdk/server/stdio.js";
import { CallToolRequestSchema, ListToolsRequestSchema } from "@modelcontextprotocol/sdk/types.js";
import { CrmClient } from "./client.js";
import { ALL_TOOLS } from "./tools/index.js";

const client = new CrmClient({
  url: process.env.PROPYTE_CRM_URL ?? "https://crm.propyte.com",
  token: process.env.CRM_MCP_API_TOKEN ?? "",
});
const server = new Server({ name: "propyte-crm-mcp", version: "0.1.0" }, { capabilities: { tools: {} } });

server.setRequestHandler(ListToolsRequestSchema, async () => ({
  tools: ALL_TOOLS.map((t) => ({ name: t.name, description: t.description, inputSchema: t.inputSchema })),
}));
server.setRequestHandler(CallToolRequestSchema, async (req) => {
  const tool = ALL_TOOLS.find((t) => t.name === req.params.name);
  if (!tool) return { isError: true, content: [{ type: "text", text: `unknown_tool: ${req.params.name}` }] };
  try {
    const result = await tool.handler(req.params.arguments ?? {}, client);
    return { content: [{ type: "text", text: JSON.stringify(result, null, 2) }] };
  } catch (e) {
    return { isError: true, content: [{ type: "text", text: (e as Error).message }] };
  }
});
const transport = new StdioServerTransport();
await server.connect(transport);
```

- [ ] **Step 2: `src/schemas.ts`** (zod de inputs F0 + F1; ampliable)

```ts
import { z } from "zod";

export const Empty = z.object({}).strict();
export const IdInput = z.object({ id: z.string().min(1) });

const actionSpec = z.object({ type: z.string(), config: z.record(z.unknown()).default({}) });

export const RuleInput = z.object({
  name: z.string().min(3).max(120),
  description: z.string().max(500).nullable().optional(),
  triggerType: z.enum(["EVENT","TIME","BEHAVIORAL","INACTIVITY","STAGE_CHANGE","SLA_BREACH","SCORE_THRESHOLD"]),
  triggerConfig: z.record(z.unknown()).default({}),
  conditions: z.record(z.unknown()).default({}),
  actions: z.array(actionSpec).min(1),
  cooldownMinutes: z.number().int().min(0).nullable().optional(),
  priority: z.number().int().min(1).max(1000).default(100),
  isActive: z.boolean().default(false),
});
export const RuleUpdateInput = RuleInput.partial().extend({ id: z.string().min(1) });

const planStep = z.object({
  order: z.number().int().min(0),
  delayMinutes: z.number().int().min(0).default(0),
  actionType: z.string(),
  config: z.record(z.unknown()).default({}),
  conditions: z.record(z.unknown()).nullable().optional(),
  autonomyLevel: z.enum(["L0","L1","L2"]).default("L0"),
});
export const PlanInput = z.object({
  name: z.string().min(3).max(120),
  isActive: z.boolean().default(false),
  ownerUserId: z.string().nullable().optional(),
  entryTrigger: z.record(z.unknown()).default({}),
  exitConditions: z.record(z.unknown()).default({}),
  steps: z.array(planStep).default([]),
});
export const PlanUpdateInput = PlanInput.partial().extend({ id: z.string().min(1) });

export const RoutingInput = z.object({
  name: z.string().min(3).max(120),
  priority: z.number().int().min(1).default(100),
  isActive: z.boolean().default(false),
  conditions: z.record(z.unknown()).default({}),
  strategy: z.enum(["ROUND_ROBIN","PERFORMANCE","MANUAL","GUARDIA"]),
  targets: z.record(z.unknown()).default({}),
});
export const RoutingUpdateInput = RoutingInput.partial().extend({ id: z.string().min(1) });

export const SlaInput = z.object({
  name: z.string().min(3).max(120),
  isDefault: z.boolean().default(false),
  firstTouchMinutes: z.number().int().min(1).default(5),
  retryMinutes: z.number().int().min(1).default(30),
  orphanHours: z.number().int().min(1).default(24),
  escalationChain: z.array(z.record(z.unknown())).default([]),
  businessHours: z.record(z.unknown()).default({}),
  channelFallback: z.record(z.unknown()).default({}),
});
export const SlaUpdateInput = SlaInput.partial().extend({ id: z.string().min(1) });
```

- [ ] **Step 3: `src/tools/introspection.ts`**

```ts
import { CrmClient } from "../client.js";
import { Empty } from "../schemas.js";

export const crmHealth = {
  name: "crm_health",
  description: "Health del CRM MCP: estado de DB y entorno.",
  inputSchema: { type: "object", properties: {}, additionalProperties: false },
  handler: async (raw: unknown, c: CrmClient) => { Empty.parse(raw); return c.request("GET", "/api/mcp/health"); },
};
export const describeSchemaTool = {
  name: "describe_schema",
  description: "Devuelve enums vivos y la forma del DSL (triggerTypes, workflowActionTypes, condiciones, routing, providers, roles). ÚSALA antes de crear flujos.",
  inputSchema: { type: "object", properties: {}, additionalProperties: false },
  handler: async (raw: unknown, c: CrmClient) => { Empty.parse(raw); return c.request("GET", "/api/mcp/schema"); },
};
export const listUsersTool = {
  name: "list_users",
  description: "Lista usuarios activos (id, nombre, email, rol) para resolver assignees/targets.",
  inputSchema: { type: "object", properties: {}, additionalProperties: false },
  handler: async (raw: unknown, c: CrmClient) => { Empty.parse(raw); return c.request("GET", "/api/mcp/users"); },
};
export const listTeamsTool = {
  name: "list_teams",
  description: "Lista equipos (id, nombre, plaza, líder).",
  inputSchema: { type: "object", properties: {}, additionalProperties: false },
  handler: async (raw: unknown, c: CrmClient) => { Empty.parse(raw); return c.request("GET", "/api/mcp/teams"); },
};
```

- [ ] **Step 4: `src/tools/index.ts`** (solo introspección por ahora)

```ts
import { crmHealth, describeSchemaTool, listUsersTool, listTeamsTool } from "./introspection.js";
export const ALL_TOOLS = [crmHealth, describeSchemaTool, listUsersTool, listTeamsTool];
```

- [ ] **Step 5: Typecheck + build**

Run: `cd ../propyte-crm-mcp && npm run typecheck && npm run build`
Expected: sin errores; genera `dist/`.

- [ ] **Step 6: Commit**

```bash
git add src/ && git commit -m "feat: server stdio + tools de introspección"
```

## Task 0.11: Smoke E2E F0

- [ ] **Step 1:** Con el CRM corriendo (local `npm run dev` o prod) y `CRM_MCP_API_TOKEN` seteado en ambos lados, probar el wrapper a mano:

Run:
```bash
cd ../propyte-crm-mcp
PROPYTE_CRM_URL=http://localhost:3000 CRM_MCP_API_TOKEN=<token> \
  node -e "import('./dist/client.js').then(async ({CrmClient})=>{const c=new CrmClient({url:process.env.PROPYTE_CRM_URL,token:process.env.CRM_MCP_API_TOKEN});console.log(await c.request('GET','/api/mcp/health'));console.log(await c.request('GET','/api/mcp/schema'));})"
```
Expected: imprime `{ ok: true, ... }` y el objeto de schema.

- [ ] **Step 2:** Probar auth negativa (token vacío → debe lanzar `unauthorized (status=401)`).

- [ ] **Step 3: Registrar el MCP** en Claude Code:

```bash
claude mcp add -s user propyte-crm -- node "C:/Users/Luis/Projects/Propyte/propyte-crm-mcp/dist/index.js"
```
Verifica que las env vars `PROPYTE_CRM_URL` y `CRM_MCP_API_TOKEN` estén en el entorno del usuario (Windows) — el proceso las hereda (ver patrón propyte-hub-mcp).

---

# FASE 1 — Flujos (caso estrella)

Todos los handlers viven en `src/lib/mcp/handlers/automation.ts`. Cada `create/update` valida con zod (reusando `conditionsDslSchema` y `workflowActionTypes` de `@/lib/validations/rebuild-f1` donde aplique), chequea duplicado por `name @unique`, persiste, y llama `writeAudit`. **Las reglas/planes nacen `isActive=false`.**

## Task 1.1: Handlers de AutomationRule

**Files:**
- Create/replace: `src/lib/mcp/handlers/automation.ts` (reemplaza los stubs de F1)
- Test: `src/lib/mcp/handlers/automation.test.ts`

- [ ] **Step 1: Test que falla** (validación de DSL + dedup; mockea prisma)

```ts
// src/lib/mcp/handlers/automation.test.ts
import { describe, it, expect, vi, beforeEach } from "vitest";

vi.mock("@/lib/db", () => ({ default: {
  automationRule: { findFirst: vi.fn(), create: vi.fn(), update: vi.fn(), findMany: vi.fn() },
  auditLog: { create: vi.fn().mockResolvedValue({}) },
} }));
import prisma from "@/lib/db";
import { createRule } from "./automation";

describe("createRule", () => {
  beforeEach(() => vi.clearAllMocks());
  it("rechaza actions vacío", async () => {
    await expect(createRule({ name: "Flujo X", triggerType: "EVENT", actions: [] }, "u1"))
      .rejects.toThrow();
  });
  it("rechaza nombre duplicado", async () => {
    (prisma.automationRule.findFirst as any).mockResolvedValue({ id: "dup" });
    await expect(createRule(
      { name: "Flujo X", triggerType: "EVENT", conditions: {}, actions: [{ type: "ASSIGN", config: {} }] }, "u1"
    )).rejects.toThrow(/ya existe/i);
  });
  it("crea regla inactiva por defecto", async () => {
    (prisma.automationRule.findFirst as any).mockResolvedValue(null);
    (prisma.automationRule.create as any).mockImplementation(({ data }: any) => ({ id: "r1", ...data }));
    const r: any = await createRule(
      { name: "Flujo X", triggerType: "EVENT", conditions: {}, actions: [{ type: "ASSIGN", config: {} }] }, "u1"
    );
    expect(r.isActive).toBe(false);
    expect(prisma.auditLog.create).toHaveBeenCalled();
  });
});
```

- [ ] **Step 2: Correr y ver fallar**

Run: `npm test -- src/lib/mcp/handlers/automation.test.ts`
Expected: FAIL.

- [ ] **Step 3: Implementar** (sección AutomationRule de `automation.ts`)

```ts
// src/lib/mcp/handlers/automation.ts
import { z } from "zod";
import prisma from "@/lib/db";
import { conditionsDslSchema, workflowActionTypes } from "@/lib/validations/rebuild-f1";
import { writeAudit } from "../respond";

const TRIGGER_TYPES = ["EVENT","TIME","BEHAVIORAL","INACTIVITY","STAGE_CHANGE","SLA_BREACH","SCORE_THRESHOLD"] as const;

const ruleSchema = z.object({
  name: z.string().min(3).max(120).trim(),
  description: z.string().max(500).nullable().optional(),
  triggerType: z.enum(TRIGGER_TYPES),
  triggerConfig: z.record(z.unknown()).default({}),
  conditions: conditionsDslSchema,
  actions: z.array(z.object({ type: z.enum(workflowActionTypes), config: z.record(z.unknown()).default({}) })).min(1),
  cooldownMinutes: z.number().int().min(0).max(43200).nullable().optional(),
  priority: z.number().int().min(1).max(1000).default(100),
  isActive: z.boolean().default(false),
});

export async function listRules() {
  return prisma.automationRule.findMany({ where: { deletedAt: null }, orderBy: { priority: "asc" } });
}
export async function getRule(id: string) {
  const r = await prisma.automationRule.findFirst({ where: { id, deletedAt: null } });
  if (!r) throw new Error("Regla no encontrada");
  return r;
}
export async function createRule(body: unknown, userId: string) {
  const d = ruleSchema.parse(body);
  if (await prisma.automationRule.findFirst({ where: { name: d.name, deletedAt: null } }))
    throw new Error("Ya existe una regla con ese nombre");
  const rule = await prisma.automationRule.create({ data: {
    name: d.name, description: d.description ?? null, triggerType: d.triggerType,
    triggerConfig: d.triggerConfig as never, conditions: d.conditions as never, actions: d.actions as never,
    cooldownMinutes: d.cooldownMinutes ?? null, priority: d.priority, isActive: d.isActive,
  } });
  await writeAudit(userId, "CREATE", "AutomationRule", rule.id, { name: d.name });
  return rule;
}
export async function updateRule(id: string, body: unknown, userId: string) {
  const existing = await prisma.automationRule.findFirst({ where: { id, deletedAt: null } });
  if (!existing) throw new Error("Regla no encontrada");
  const d = ruleSchema.partial().parse(body);
  if (d.name && await prisma.automationRule.findFirst({ where: { name: d.name, deletedAt: null, id: { not: id } } }))
    throw new Error("Ya existe otra regla con ese nombre");
  const rule = await prisma.automationRule.update({ where: { id }, data: {
    ...(d.name !== undefined ? { name: d.name } : {}),
    ...(d.description !== undefined ? { description: d.description } : {}),
    ...(d.triggerType !== undefined ? { triggerType: d.triggerType } : {}),
    ...(d.triggerConfig !== undefined ? { triggerConfig: d.triggerConfig as never } : {}),
    ...(d.conditions !== undefined ? { conditions: d.conditions as never } : {}),
    ...(d.actions !== undefined ? { actions: d.actions as never } : {}),
    ...(d.cooldownMinutes !== undefined ? { cooldownMinutes: d.cooldownMinutes } : {}),
    ...(d.priority !== undefined ? { priority: d.priority } : {}),
    ...(d.isActive !== undefined ? { isActive: d.isActive } : {}),
  } });
  await writeAudit(userId, "UPDATE", "AutomationRule", rule.id, { name: rule.name, changed: Object.keys(d) });
  return rule;
}
```

- [ ] **Step 4: Correr y ver pasar**

Run: `npm test -- src/lib/mcp/handlers/automation.test.ts`
Expected: PASS (3 tests).

- [ ] **Step 5: Commit**

```bash
git add src/lib/mcp/handlers/automation.ts src/lib/mcp/handlers/automation.test.ts
git commit -m "feat(mcp): handlers de AutomationRule (list/get/create/update)"
```

## Task 1.2: Handlers de ActionPlan, Routing, SLA, Queue

**Files:**
- Modify: `src/lib/mcp/handlers/automation.ts` (agregar secciones)

- [ ] **Step 1: Implementar las secciones** (verifica nombres de campos contra `prisma/schema.prisma`):

```ts
// --- ActionPlan ---
export async function listPlans() {
  return prisma.actionPlan.findMany({ where: { deletedAt: null }, include: { steps: { orderBy: { order: "asc" } } } });
}
export async function getPlan(id: string) {
  const p = await prisma.actionPlan.findFirst({ where: { id, deletedAt: null }, include: { steps: { orderBy: { order: "asc" } } } });
  if (!p) throw new Error("Plan no encontrado");
  return p;
}
const planSchema = z.object({
  name: z.string().min(3).max(120).trim(),
  isActive: z.boolean().default(false),
  ownerUserId: z.string().nullable().optional(),
  entryTrigger: z.record(z.unknown()).default({}),
  exitConditions: z.record(z.unknown()).default({}),
  steps: z.array(z.object({
    order: z.number().int().min(0),
    delayMinutes: z.number().int().min(0).default(0),
    actionType: z.enum(workflowActionTypes),
    config: z.record(z.unknown()).default({}),
    conditions: z.record(z.unknown()).nullable().optional(),
    autonomyLevel: z.enum(["L0","L1","L2"]).default("L0"),
  })).default([]),
});
export async function createPlan(body: unknown, userId: string) {
  const d = planSchema.parse(body);
  if (await prisma.actionPlan.findFirst({ where: { name: d.name, deletedAt: null } }))
    throw new Error("Ya existe un plan con ese nombre");
  const plan = await prisma.actionPlan.create({ data: {
    name: d.name, isActive: d.isActive, ownerUserId: d.ownerUserId ?? null,
    entryTrigger: d.entryTrigger as never, exitConditions: d.exitConditions as never,
    steps: { create: d.steps.map((s) => ({
      order: s.order, delayMinutes: s.delayMinutes, actionType: s.actionType,
      config: s.config as never, conditions: (s.conditions ?? null) as never, autonomyLevel: s.autonomyLevel,
    })) },
  }, include: { steps: true } });
  await writeAudit(userId, "CREATE", "ActionPlan", plan.id, { name: d.name, steps: d.steps.length });
  return plan;
}
export async function updatePlan(id: string, body: unknown, userId: string) {
  const existing = await prisma.actionPlan.findFirst({ where: { id, deletedAt: null } });
  if (!existing) throw new Error("Plan no encontrado");
  const d = planSchema.partial().parse(body);
  // Si vienen steps, reemplaza el set completo (borra steps viejos y recrea).
  const plan = await prisma.$transaction(async (tx) => {
    if (d.steps) {
      await tx.actionPlanStep.deleteMany({ where: { planId: id } });
      await tx.actionPlanStep.createMany({ data: d.steps.map((s) => ({
        planId: id, order: s.order, delayMinutes: s.delayMinutes ?? 0, actionType: s.actionType,
        config: (s.config ?? {}) as never, conditions: (s.conditions ?? null) as never, autonomyLevel: s.autonomyLevel ?? "L0",
      })) });
    }
    return tx.actionPlan.update({ where: { id }, data: {
      ...(d.name !== undefined ? { name: d.name } : {}),
      ...(d.isActive !== undefined ? { isActive: d.isActive } : {}),
      ...(d.ownerUserId !== undefined ? { ownerUserId: d.ownerUserId } : {}),
      ...(d.entryTrigger !== undefined ? { entryTrigger: d.entryTrigger as never } : {}),
      ...(d.exitConditions !== undefined ? { exitConditions: d.exitConditions as never } : {}),
    }, include: { steps: { orderBy: { order: "asc" } } } });
  });
  await writeAudit(userId, "UPDATE", "ActionPlan", id, { changed: Object.keys(d) });
  return plan;
}

// --- RoutingRule ---
const routingSchema = z.object({
  name: z.string().min(3).max(120).trim(),
  priority: z.number().int().min(1).max(1000).default(100),
  isActive: z.boolean().default(false),
  conditions: z.record(z.unknown()).default({}),
  strategy: z.enum(["ROUND_ROBIN","PERFORMANCE","MANUAL","GUARDIA"]),
  targets: z.record(z.unknown()).default({}),
});
export async function listRouting() { return prisma.routingRule.findMany({ orderBy: { priority: "asc" } }); }
export async function createRouting(body: unknown, userId: string) {
  const d = routingSchema.parse(body);
  if (await prisma.routingRule.findFirst({ where: { name: d.name } }))
    throw new Error("Ya existe una regla de ruteo con ese nombre");
  const r = await prisma.routingRule.create({ data: {
    name: d.name, priority: d.priority, isActive: d.isActive,
    conditions: d.conditions as never, strategy: d.strategy, targets: d.targets as never,
  } });
  await writeAudit(userId, "CREATE", "RoutingRule", r.id, { name: d.name });
  return r;
}
export async function updateRouting(id: string, body: unknown, userId: string) {
  const existing = await prisma.routingRule.findUnique({ where: { id } });
  if (!existing) throw new Error("Regla de ruteo no encontrada");
  const d = routingSchema.partial().parse(body);
  const r = await prisma.routingRule.update({ where: { id }, data: {
    ...(d.name !== undefined ? { name: d.name } : {}),
    ...(d.priority !== undefined ? { priority: d.priority } : {}),
    ...(d.isActive !== undefined ? { isActive: d.isActive } : {}),
    ...(d.conditions !== undefined ? { conditions: d.conditions as never } : {}),
    ...(d.strategy !== undefined ? { strategy: d.strategy } : {}),
    ...(d.targets !== undefined ? { targets: d.targets as never } : {}),
  } });
  await writeAudit(userId, "UPDATE", "RoutingRule", id, { changed: Object.keys(d) });
  return r;
}

// --- SlaPolicy ---
const slaSchema = z.object({
  name: z.string().min(3).max(120).trim(),
  isDefault: z.boolean().default(false),
  firstTouchMinutes: z.number().int().min(1).default(5),
  retryMinutes: z.number().int().min(1).default(30),
  orphanHours: z.number().int().min(1).default(24),
  escalationChain: z.array(z.record(z.unknown())).default([]),
  businessHours: z.record(z.unknown()).default({}),
  channelFallback: z.record(z.unknown()).default({}),
});
export async function listSla() { return prisma.slaPolicy.findMany({ orderBy: { name: "asc" } }); }
export async function createSla(body: unknown, userId: string) {
  const d = slaSchema.parse(body);
  if (await prisma.slaPolicy.findFirst({ where: { name: d.name } }))
    throw new Error("Ya existe una política SLA con ese nombre");
  const s = await prisma.slaPolicy.create({ data: {
    name: d.name, isDefault: d.isDefault, firstTouchMinutes: d.firstTouchMinutes,
    retryMinutes: d.retryMinutes, orphanHours: d.orphanHours,
    escalationChain: d.escalationChain as never, businessHours: d.businessHours as never, channelFallback: d.channelFallback as never,
  } });
  await writeAudit(userId, "CREATE", "SlaPolicy", s.id, { name: d.name });
  return s;
}
export async function updateSla(id: string, body: unknown, userId: string) {
  const existing = await prisma.slaPolicy.findUnique({ where: { id } });
  if (!existing) throw new Error("Política SLA no encontrada");
  const d = slaSchema.partial().parse(body);
  const s = await prisma.slaPolicy.update({ where: { id }, data: {
    ...(d.name !== undefined ? { name: d.name } : {}),
    ...(d.isDefault !== undefined ? { isDefault: d.isDefault } : {}),
    ...(d.firstTouchMinutes !== undefined ? { firstTouchMinutes: d.firstTouchMinutes } : {}),
    ...(d.retryMinutes !== undefined ? { retryMinutes: d.retryMinutes } : {}),
    ...(d.orphanHours !== undefined ? { orphanHours: d.orphanHours } : {}),
    ...(d.escalationChain !== undefined ? { escalationChain: d.escalationChain as never } : {}),
    ...(d.businessHours !== undefined ? { businessHours: d.businessHours as never } : {}),
    ...(d.channelFallback !== undefined ? { channelFallback: d.channelFallback as never } : {}),
  } });
  await writeAudit(userId, "UPDATE", "SlaPolicy", id, { changed: Object.keys(d) });
  return s;
}

// --- ActionQueue (observabilidad + retry) ---
export async function listQueue() {
  return prisma.actionQueue.findMany({ orderBy: { runAfter: "desc" }, take: 100 });
}
export async function retryQueue(id: string, userId: string) {
  const item = await prisma.actionQueue.findUnique({ where: { id } });
  if (!item) throw new Error("Item de cola no encontrado");
  if (item.status !== "FAILED") throw new Error("Solo se puede reintentar items FAILED");
  const updated = await prisma.actionQueue.update({ where: { id }, data: { status: "PENDING", attempts: 0, error: null, runAfter: new Date() } });
  await writeAudit(userId, "UPDATE", "ActionQueue", id, { action: "retry" });
  return updated;
}
```

> **Verificación obligatoria antes de implementar:** abre `prisma/schema.prisma` y confirma los nombres exactos de modelos/campos (`ActionPlan.steps` relación, `ActionPlanStep.planId`, `RoutingRule`, `SlaPolicy`, `ActionQueue.status` enum, soft-delete `deletedAt` en cuáles existe). Ajusta donde difiera.

- [ ] **Step 2: Test de routing/sla** (mock prisma, mismo patrón que 1.1)

```ts
// añadir a automation.test.ts
import { createRouting, createSla } from "./automation";
// (extiende el vi.mock con routingRule y slaPolicy: { findFirst, findUnique, create, update, findMany })
it("routing rechaza duplicado", async () => {
  (prisma as any).routingRule = { findFirst: vi.fn().mockResolvedValue({ id: "x" }), create: vi.fn() };
  await expect(createRouting({ name: "Ruteo TULUM", strategy: "ROUND_ROBIN" }, "u1")).rejects.toThrow(/ya existe/i);
});
```

- [ ] **Step 3: Correr toda la suite**

Run: `npm test`
Expected: PASS (incluye auth, dispatch, automation).

- [ ] **Step 4: Commit**

```bash
git add src/lib/mcp/handlers/automation.ts src/lib/mcp/handlers/automation.test.ts
git commit -m "feat(mcp): handlers de ActionPlan/Routing/SLA/Queue"
```

## Task 1.3: Tools del wrapper para flujos

**Files:**
- Create: `../propyte-crm-mcp/src/tools/automation.ts`
- Modify: `../propyte-crm-mcp/src/tools/index.ts`

- [ ] **Step 1: `src/tools/automation.ts`**

```ts
import { CrmClient } from "../client.js";
import { Empty, IdInput, RuleInput, RuleUpdateInput, PlanInput, PlanUpdateInput,
  RoutingInput, RoutingUpdateInput, SlaInput, SlaUpdateInput } from "../schemas.js";

const obj = (props: Record<string, unknown>, required: string[] = []) =>
  ({ type: "object", properties: props, required, additionalProperties: true });

export const listRules = {
  name: "list_automation_rules", description: "Lista todas las reglas de automatización.",
  inputSchema: obj({}), handler: async (r: unknown, c: CrmClient) => { Empty.parse(r); return c.request("GET", "/api/mcp/automation/rules"); },
};
export const getRule = {
  name: "get_automation_rule", description: "Detalle de una regla por id.",
  inputSchema: obj({ id: { type: "string" } }, ["id"]),
  handler: async (r: unknown, c: CrmClient) => { const { id } = IdInput.parse(r); return c.request("GET", `/api/mcp/automation/rules/${encodeURIComponent(id)}`); },
};
export const createRule = {
  name: "create_automation_rule",
  description: "Crea una regla de automatización (nace inactiva). triggerType ∈ describe_schema.triggerTypes; actions = [{type: WorkflowActionType, config}]. Llama describe_schema primero.",
  inputSchema: obj({
    name: { type: "string" }, description: { type: "string" },
    triggerType: { type: "string" }, triggerConfig: { type: "object" },
    conditions: { type: "object" }, actions: { type: "array" },
    cooldownMinutes: { type: "number" }, priority: { type: "number" }, isActive: { type: "boolean" },
  }, ["name", "triggerType", "actions"]),
  handler: async (r: unknown, c: CrmClient) => c.request("POST", "/api/mcp/automation/rules", RuleInput.parse(r)),
};
export const updateRule = {
  name: "update_automation_rule", description: "Actualiza/activa una regla (incluye isActive para encender/apagar).",
  inputSchema: obj({ id: { type: "string" }, isActive: { type: "boolean" } }, ["id"]),
  handler: async (r: unknown, c: CrmClient) => { const d = RuleUpdateInput.parse(r); return c.request("PATCH", `/api/mcp/automation/rules/${encodeURIComponent(d.id)}`, d); },
};

export const listPlans = { name: "list_action_plans", description: "Lista cadencias/planes de acción con sus pasos.", inputSchema: obj({}),
  handler: async (r: unknown, c: CrmClient) => { Empty.parse(r); return c.request("GET", "/api/mcp/automation/plans"); } };
export const getPlan = { name: "get_action_plan", description: "Detalle de un plan por id.", inputSchema: obj({ id: { type: "string" } }, ["id"]),
  handler: async (r: unknown, c: CrmClient) => { const { id } = IdInput.parse(r); return c.request("GET", `/api/mcp/automation/plans/${encodeURIComponent(id)}`); } };
export const createPlan = { name: "create_action_plan", description: "Crea un plan de acción con steps (nace inactivo).",
  inputSchema: obj({ name: { type: "string" }, steps: { type: "array" }, isActive: { type: "boolean" }, ownerUserId: { type: "string" }, entryTrigger: { type: "object" }, exitConditions: { type: "object" } }, ["name"]),
  handler: async (r: unknown, c: CrmClient) => c.request("POST", "/api/mcp/automation/plans", PlanInput.parse(r)) };
export const updatePlan = { name: "update_action_plan", description: "Actualiza un plan (si envías steps, reemplaza el set completo).",
  inputSchema: obj({ id: { type: "string" } }, ["id"]),
  handler: async (r: unknown, c: CrmClient) => { const d = PlanUpdateInput.parse(r); return c.request("PATCH", `/api/mcp/automation/plans/${encodeURIComponent(d.id)}`, d); } };

export const listRouting = { name: "list_routing_rules", description: "Lista reglas de ruteo de leads.", inputSchema: obj({}),
  handler: async (r: unknown, c: CrmClient) => { Empty.parse(r); return c.request("GET", "/api/mcp/automation/routing"); } };
export const createRouting = { name: "create_routing_rule", description: "Crea regla de ruteo. strategy ∈ ROUND_ROBIN|PERFORMANCE|MANUAL|GUARDIA. targets={userIds,roles,weights}.",
  inputSchema: obj({ name: { type: "string" }, strategy: { type: "string" }, conditions: { type: "object" }, targets: { type: "object" }, priority: { type: "number" }, isActive: { type: "boolean" } }, ["name", "strategy"]),
  handler: async (r: unknown, c: CrmClient) => c.request("POST", "/api/mcp/automation/routing", RoutingInput.parse(r)) };
export const updateRouting = { name: "update_routing_rule", description: "Actualiza/activa una regla de ruteo.", inputSchema: obj({ id: { type: "string" } }, ["id"]),
  handler: async (r: unknown, c: CrmClient) => { const d = RoutingUpdateInput.parse(r); return c.request("PATCH", `/api/mcp/automation/routing/${encodeURIComponent(d.id)}`, d); } };

export const listSla = { name: "list_sla_policies", description: "Lista políticas SLA.", inputSchema: obj({}),
  handler: async (r: unknown, c: CrmClient) => { Empty.parse(r); return c.request("GET", "/api/mcp/automation/sla"); } };
export const createSla = { name: "create_sla_policy", description: "Crea política SLA (firstTouch/retry/orphan + escalationChain).",
  inputSchema: obj({ name: { type: "string" }, firstTouchMinutes: { type: "number" }, retryMinutes: { type: "number" }, orphanHours: { type: "number" }, escalationChain: { type: "array" }, businessHours: { type: "object" }, isDefault: { type: "boolean" } }, ["name"]),
  handler: async (r: unknown, c: CrmClient) => c.request("POST", "/api/mcp/automation/sla", SlaInput.parse(r)) };
export const updateSla = { name: "update_sla_policy", description: "Actualiza una política SLA.", inputSchema: obj({ id: { type: "string" } }, ["id"]),
  handler: async (r: unknown, c: CrmClient) => { const d = SlaUpdateInput.parse(r); return c.request("PATCH", `/api/mcp/automation/sla/${encodeURIComponent(d.id)}`, d); } };

export const listQueue = { name: "list_action_queue", description: "Lista los últimos 100 items de la cola de acciones (observabilidad).", inputSchema: obj({}),
  handler: async (r: unknown, c: CrmClient) => { Empty.parse(r); return c.request("GET", "/api/mcp/automation/queue"); } };
export const retryAction = { name: "retry_action", description: "Reencola un item FAILED de la cola.", inputSchema: obj({ id: { type: "string" } }, ["id"]),
  handler: async (r: unknown, c: CrmClient) => { const { id } = IdInput.parse(r); return c.request("POST", `/api/mcp/automation/queue/${encodeURIComponent(id)}/retry`); } };
```

- [ ] **Step 2: Actualizar `src/tools/index.ts`**

```ts
import { crmHealth, describeSchemaTool, listUsersTool, listTeamsTool } from "./introspection.js";
import * as wf from "./automation.js";
export const ALL_TOOLS = [
  crmHealth, describeSchemaTool, listUsersTool, listTeamsTool,
  wf.listRules, wf.getRule, wf.createRule, wf.updateRule,
  wf.listPlans, wf.getPlan, wf.createPlan, wf.updatePlan,
  wf.listRouting, wf.createRouting, wf.updateRouting,
  wf.listSla, wf.createSla, wf.updateSla,
  wf.listQueue, wf.retryAction,
];
```

- [ ] **Step 3: Test del wrapper** (que cada tool tiene name/description/inputSchema/handler y que `create_automation_rule` arma el path correcto con un fetch mock)

```ts
// tests/tools.test.ts
import { test } from "node:test";
import assert from "node:assert";
import { ALL_TOOLS } from "../src/tools/index.js";
import { CrmClient } from "../src/client.js";

test("todas las tools tienen forma válida", () => {
  for (const t of ALL_TOOLS) {
    assert.ok(t.name && t.description && t.inputSchema && typeof t.handler === "function");
  }
});
test("create_automation_rule pega POST a /api/mcp/automation/rules", async () => {
  let captured: any = {};
  const fakeFetch = async (url: string, init: any) => {
    captured = { url, method: init.method, body: JSON.parse(init.body) };
    return { ok: true, status: 201, text: async () => JSON.stringify({ data: { id: "r1" } }) } as any;
  };
  const c = new CrmClient({ url: "http://x", token: "t", fetchImpl: fakeFetch as any });
  const tool = ALL_TOOLS.find((t) => t.name === "create_automation_rule")!;
  const res = await tool.handler({ name: "Flujo X", triggerType: "EVENT", actions: [{ type: "ASSIGN", config: {} }] }, c);
  assert.equal(captured.method, "POST");
  assert.ok(captured.url.endsWith("/api/mcp/automation/rules"));
  assert.deepEqual(res, { id: "r1" });
});
```

- [ ] **Step 4: Correr typecheck + tests + build**

Run: `cd ../propyte-crm-mcp && npm run typecheck && npm test && npm run build`
Expected: tests PASS, build OK.

- [ ] **Step 5: Commit**

```bash
git add src/tools/ tests/ && git commit -m "feat: tools de flujos (rules/plans/routing/sla/queue)"
```

## Task 1.4: Smoke E2E del caso estrella

- [ ] **Step 1:** Reiniciar el MCP en Claude Code (recarga) y pedirle en lenguaje natural: *"crea un flujo: cuando entre un lead de Meta en TULUM, asígnalo round-robin al equipo TULUM y mándale WhatsApp de bienvenida"*. Claude debe: `describe_schema` → `list_teams`/`list_users` → `create_automation_rule` (isActive=false).

- [ ] **Step 2:** Verificar en DB que la regla existe inactiva y que hay fila en `audit_logs` con `changes.source='mcp'`.

Run (en CRM): `npx tsx -e "import p from './src/lib/db'; p.automationRule.findMany({where:{name:{contains:'Meta'}}}).then(r=>{console.log(r);process.exit(0)})"`
Expected: la regla creada, `isActive: false`.

- [ ] **Step 3:** Activarla vía `update_automation_rule { id, isActive: true }` y confirmar que el cron `/api/cron/workflows` la considera en el siguiente tick (revisar logs / `list_action_queue`).

- [ ] **Step 4:** Cleanup: `update_automation_rule { id, isActive: false }` (no hay DELETE por diseño).

---

## Self-Review (hecho)

- **Cobertura del spec:** F0 (route+auth+usuario-sistema+introspección+wrapper) y F1 (rules/plans/routing/sla/queue + tools) cubiertos. F2/F3/F4 → planes separados (fuera de este documento, declarado en el scope check).
- **Sin DELETE:** el route no exporta DELETE; no hay tool de borrado; reversibles vía isActive.
- **Consistencia de tipos:** nombres de handler (`listRules/createRule/...`) coinciden entre `dispatch.ts`, `automation.ts` y las tools. Paths REST idénticos en route, dispatch y client.
- **Placeholders:** ninguno; los `> Nota`/`> Verificación` son chequeos contra `prisma/schema.prisma` reales que el ejecutor DEBE confirmar (campos de Team/ActionPlan/ActionQueue), no código faltante.

## Riesgo conocido a verificar en ejecución
Los nombres exactos de campos de `Team`, `ActionPlan`/`ActionPlanStep`, `RoutingRule`, `SlaPolicy`, `ActionQueue` y qué modelos tienen `deletedAt` deben confirmarse contra `prisma/schema.prisma` antes de cada handler. El plan asume los del spec (mapa del Explore); si difieren, ajustar el `select`/`data` correspondiente.
